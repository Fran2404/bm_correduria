{% extends 'base.html.twig' %}

{% block title %}Poliza{% endblock %}

{% block body %}
    <h1>Poliza</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ poliza.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ poliza.numero }}</td>
            </tr>
            <tr>
                <th>FechaVencimiento</th>
                <td>{{ poliza.fechaVencimiento ? poliza.fechaVencimiento|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>TipoSeguro</th>
                <td>{{ poliza.tipoSeguro }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_poliza_index') }}">back to list</a>

    <a href="{{ path('app_admin_poliza_edit', {'id': poliza.id}) }}">edit</a>

    {{ include('admin_poliza/_delete_form.html.twig') }}
{% endblock %}
