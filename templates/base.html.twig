<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Bienvenido a BM Correduría{% endblock %}</title>
        <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container">
                <a class="navbar-brand" href="{{ path('app_home') }}">BM Correduría</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_about') }}">Sobre Nosotros</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_contact') }}">Contacto</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_products') }}">Productos</a></li>
                    </ul>
                </div>
            </div>
        </nav>
        <div class="container mt-4">
            {% block body %}{% endblock %}
        </div>
        <footer class="bg-light py-3 mt-4">
            <div class="container text-center">
                <p>© 2025 BM Correduría de Seguros. Todos los derechos reservados.</p>
                <a href="{{ path('app_contact') }}" class="text-muted">Contacto</a>
            </div>
        </footer>
        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        {% endblock %}
    </body>
</html>
