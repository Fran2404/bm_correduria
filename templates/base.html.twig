<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Bienvenido a Beltrán Méndez Correduría{% endblock %}</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
        <style>
            html {
                height: 100%;
                margin: 0;
                padding: 0;
            }
            body {
                display: flex;
                flex-direction: column;
                min-height: 100vh;
                margin: 0;
                padding: 0;
                padding-bottom: 60px; /* Espacio para el footer fijo */
                background-color: #F9FAFB; /* Fondo claro para todo el cuerpo */
            }
            .content {
                flex: 1 0 auto; /* Asegura que el contenido ocupe el espacio disponible */
            }
            footer {
                position: fixed; /* Fija el footer al final */
                bottom: 0;
                left: 0;
                width: 100%;
                background-color: #1E3A8A; /* Azul oscuro del logo */
                color: white;
                padding: 0.5rem 0;
                text-align: center;
                z-index: 1000; /* Asegura que el footer esté por encima de otros elementos */
            }
            footer p {
                margin: 0;
                font-size: 0.9rem;
                display: inline;
            }
            footer a {
                color: #E0E7FF; /* Aseguramos que el enlace sea visible */
                text-decoration: underline;
                font-size: 0.9rem;
                margin-left: 10px;
            }
            footer a:hover {
                color: #A3BFFA; /* Cambio suave al pasar el ratón */
            }
            /* Barra de navegación */
            .navbar {
                background-color: #1E3A8A; /* Azul oscuro del logo */
                padding: 0.5rem 1rem;
                height: 60px;
            }
            .navbar .navbar-brand, .navbar .nav-link {
                color: white !important;
                font-size: 1rem;
            }
            .navbar .nav-link:hover {
                color: #A3BFFA !important; /* Tono más claro al pasar el ratón */
            }
            /* Estilos para el carrusel */
            .carousel {
                margin-bottom: 20px;
                border: 2px solid #1E3A8A;
                border-radius: 10px;
                overflow: hidden;
            }
            .carousel-item img {
                max-height: 400px; /* Mantendremos este valor, pero el título se reducirá */
                object-fit: cover;
                width: 100%;
                display: block;
            }
            /* Botones */
            .btn-outline-primary {
                color: #1E3A8A;
                border-color: #1E3A8A;
            }
            .btn-outline-primary:hover {
                background-color: #1E3A8A;
                color: white;
            }
            .btn-primary {
                background-color: #1E3A8A;
                border-color: #1E3A8A;
            }
            .btn-primary:hover {
                background-color: #163172;
            }
            /* Ajustes para responsive */
            @media (max-width: 768px) {
                .carousel-item img {
                    max-height: 250px;
                }
                .display-4 {
                    font-size: 2rem; /* Reducimos el tamaño del título */
                }
                .lead {
                    font-size: 1rem;
                }
                .btn-lg {
                    font-size: 0.9rem;
                    padding: 0.5rem 1rem;
                }
                .navbar {
                    height: 50px;
                }
                body {
                    padding-bottom: 40px; /* Reducimos el padding para el footer */
                }
            }
            @media (max-width: 576px) {
                .carousel-item img {
                    max-height: 200px;
                }
                .display-4 {
                    font-size: 1.5rem; /* Reducimos aún más para móviles */
                }
                .lead {
                    font-size: 0.9rem;
                }
                .btn-lg {
                    font-size: 0.8rem;
                    padding: 0.4rem 0.8rem;
                }
                .navbar {
                    height: 40px;
                }
                body {
                    padding-bottom: 30px; /* Reducimos aún más para móviles */
                }
            }
            /* Márgenes razonables */
            .container.mt-4 {
                margin-top: 1.5rem !important;
            }
            hr.my-4 {
                margin: 1rem 0 !important;
            }
            .welcome-message {
                font-size: 1.2rem;
                font-weight: 500;
                color: #1E3A8A;
                margin-bottom: 1rem;
            }
            /* Ajustamos el tamaño del título */
            .display-4 {
                font-size: 2.5rem; /* Reducimos el tamaño del título para pantallas grandes */
            }
        </style>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-light">
            <div class="container">
                <a class="navbar-brand" href="{{ path('app_home') }}">BM Correduría</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_about') }}">Sobre Nosotros</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_contact') }}">Contacto</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_products') }}">Productos</a></li>
                    </ul>
                    <ul class="navbar-nav ms-auto">
                        {% if app.user %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_logout') }}">Cerrar sesión</a>
                            </li>
                        {% else %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_login') }}">Iniciar sesión</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_register') }}">Registrarse</a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>
        <div class="content">
            <div class="container mt-4">
                {% block body %}{% endblock %}
            </div>
        </div>
        <footer>
            <div class="container text-center">
                <p style="display: inline;">© 2025 BM Correduría de Seguros. Todos los derechos reservados.</p>
                <a href="{{ path('app_contact') }}">Contacto</a>
            </div>
        </footer>
        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        {% endblock %}
    </body>
</html>
